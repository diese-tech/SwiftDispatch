This pass fixed the main first-time onboarding blockers by adding superadmin-side company admin creation, correcting the superadmin technician creation path to match the live schema, and adding a repeatable `npm run seed:live-qa` script that ensures a stable test tenant, admin, dispatcher, technicians, and quote template for tomorrow’s walkthrough; `npm test` and `npm run build` both passed, the superadmin browser flow is working, and the next thing to do in the morning is use that seeded setup to run the full browser-only admin → dispatcher → technician story while treating the current localhost E2E timeouts as a local dev-process/network issue to stabilize separately if they still reproduce.
