# SwiftDispatch – Claude Notes

## Investigation discipline

Before pushing back on a user's claim about a deprecated API or conflicting file,
run a broad file search (e.g. `find src -name "*.ts"`) to check for all relevant
files. Do not conclude something is fine based on reading only one file when the
user has flagged a potential conflict.
